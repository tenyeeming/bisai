# AcuNavi 比賽 demo 網站

資服競賽（InnoServe 第 31 屆）用的展示版。**手部穴道即時定位 + 雙手按壓確認 + 收集養成**。

> 來源：`穴道按摩正式版本/网页版_2d/acunavi-ideal.html` 的快照（2026-08-09 複製，定位公式 v35）。
> 之後兩邊各自演進，**比賽線只改這個資料夾**。
> 🧩 **2026-09-21：App 多了一組網頁沒有的東西 —— 前臂（手肘）15 穴（App 批 57）。**
> 用戶指示「不用先走 app」，所以這批**直接進 App、網頁沒動**（與既有的「網頁先行」流程相反）。
> 網頁若要跟進，要補：第三組圖冊（49 → 64）、選穴頁手肘分頁、症狀 16 → 20 類、15 張示意圖。
> 細節見 `比賽專區/AcuNavi/README.md` 的〈2026-09-21 — 前臂（手肘）15 穴進 App（批 57）〉。
> 🖼️ **2026-09-23：臉部頁提示疊進取景框，App 先做了、網頁沒跟（App 批 60）。**
> 網頁的手部頁批 59 已把讀數條搬進 `.viewport`，但臉部頁 `#face-gate` 當時刻意沒動 ——
> 現在 App 的臉部頁改了，**這一頁兩邊不一致**。網頁要跟進就照手部頁的做法搬 `#face-gate`。

---

## 怎麼開

**雙擊 `acunavi-ideal.html`。** 沒了，不用起伺服器。

如果攝像頭在 `file://` 下出問題（見〈已知風險〉），改雙擊 **`啟動.bat`**——
它會起本機伺服器並自動開瀏覽器，關掉黑視窗就停止。

> **決賽現場請用 `啟動.bat`**，因為只有它會走本機的 MediaPipe、完全不碰網路。
> 雙擊開的那條路徑仍然要連網。理由見〈MediaPipe 離線化〉。

---

## 線上部署（GitHub Pages）

線上版倉庫是 **獨立的** `https://github.com/tenyeeming/bisai`（不是主倉庫 xuedao），
只放這個資料夾的內容，網址 **https://tenyeeming.github.io/bisai/**。

### 怎麼更新線上版

```powershell
.\比賽專區\發布\部署.ps1                      # 一鍵：同步 + commit + push
.\比賽專區\發布\部署.ps1 -Message "改了什麼"   # 自訂 commit 訊息
```

它會把 `demo網站\` 整包 robocopy 到 `.deploy-bisai\` 工作區（`/XD` 只排除
`node_modules`、`/XF` 排除 `package-lock.json`），換上 `發布\` 那份給評審看的
README 與 `index.html`，補 `.nojekyll`，然後 commit + push。
這份開發用 README 在線上叫 `開發說明.md`。

**不會 push 的**：`node_modules/`、`package-lock.json`。

> 🚨 **2026-09-20 起已知：這支腳本會在 commit 之前中斷。**
> 它設了 `$ErrorActionPreference = "Stop"`，而 `git add -A` 會把
> `warning: ... LF will be replaced by CRLF` 寫到 **stderr** ⇒ PowerShell 5.1 包成
> `NativeCommandError` ⇒ **robocopy 同步做完了、commit 與 push 沒跑**。
> 症狀是「跑完沒報錯的樣子，但線上沒變」。
> **跑完一定要自己確認 `git -C .deploy-bisai log -1` 有沒有新 commit**，
> 沒有就手動 `git commit` + `git push origin main` 接完。（修法：git 呼叫包 try/catch。）

### ⚠️ 2026-08-13 變更：`vendor/` 現在**會**上線

以前這裡寫「vendor 不要 push，線上靠 `onerror` 退回 CDN」。**那個狀態已經結束。**

原因是查出來一件事：線上版每台手機第一次按「開始定位」，要從 jsDelivr 抓 **約 16MB**
（`simd_wasm_bin.wasm` 6.1MB ＋ `packed_assets.data` 4.2MB ＋ `hand_landmark_full.tflite` 5.4MB），
4G 上 10–30 秒、而且中間沒有進度提示 —— 使用者的體感是「點下去卡死」，
這是網頁版最痛的一段，比掉幀嚴重得多。

`部署.ps1` 的 robocopy `/E` 本來就會把 vendor 帶上去（`/XD` 只排 `node_modules`），
之前線上沒有純粹是因為 **vendor 是 08-13 才下載的，而上一次部署停在 08-10**。
2026-08-13 重跑部署後，bisai 上已有 22 個 vendor 檔（已驗線上抓得到 `hands.js`）。

- 代價：public repo 多約 21MB（壓縮後）。
- 好處：線上版**不再依賴 jsDelivr**，決賽現場網路不穩也不會整個定位掛掉。
- ⚠️ 之後每次部署都會帶著 vendor，這是預期行為，不要再「順手排除」它。

### ⚠️ 2026-08-13 踩到的坑：push 了但打不開

檔案早就 push 成功，`https://tenyeeming.github.io/bisai/` 卻一直 404。
原因是 **GitHub Pages 從來沒被啟用**（`has_pages: false`）——
push 只是把檔案放上去，**Pages 要另外去 repo 設定裡手動開一次**：

> Settings → Pages → Source 選 **Deploy from a branch** → 分支 `main`、資料夾 `/ (root)` → Save

開過一次之後，以後 push 就會自動重新部署，不用再點。
排查指令（不用開瀏覽器就知道是不是這個問題）：

```bash
curl -s https://api.github.com/repos/tenyeeming/bisai | grep has_pages
```

`index.html` 只是轉址到 `acunavi-ideal.html`；`.nojekyll` 是叫 Pages 不要跑 Jekyll
（否則底線開頭的檔案如 `assets/minions/_generator.js` 會被吃掉）。兩個都別刪。

---

## MediaPipe 離線化（2026-08-13）

**現場網路不穩的話，走 CDN 的 wasm 一抓不到整個定位就掛**，而決賽最重的那一格
「可行性 35%」括號裡寫的就是穩定性。所以要能離線——但不能因此把「雙擊就開」弄壞。

### 先做這件事

```bash
node vendor/下載.js      # 約 40MB，跑一次就好，要網路
```

抓下來的東西放 `vendor/mediapipe/`。沒跑也不會壞，只是每一項都退回 CDN。

**版控狀態（2026-08-13 起分兩邊，不一樣）：**

| repo | vendor 進不進版控 | 為什麼 |
|---|---|---|
| 主 repo `xuedao`（private） | **不進** | Google 的著作，且是可重建的下載物 —— 跟 NanoDet 建置產物、`.task` 解包權重同一套處理 |
| 線上 `bisai`（public，Pages） | **進**（08-13 起） | 線上版首載 16MB 走 CDN 是最痛的卡點，見上面「vendor 現在會上線」 |

所以在主 repo 這邊，`vendor/` 仍然是「自己跑 `下載.js` 生出來」的東西，不要 `git add`；
但 `部署.ps1` 會把它複製進 `.deploy-bisai\` 並推上線。兩邊行為不同是刻意的。

### 為什麼分成兩層

| 載什麼 | 從哪裡 | 為什麼 |
|---|---|---|
| 那三支 `.js` | **一律先讀本機**，讀不到才用 `onerror` 退回 CDN | `<script src>` 不受 CORS 管，`file://` 底下也讀得到 |
| wasm / tflite / `.data` | **只有用 http(s) 開**才吃本機檔 | MediaPipe 內部是用 `fetch` 抓這些的，而 `file://` 是匿名來源，`fetch` 讀鄰居檔案一律被擋 |

所以：

```
雙擊 acunavi-ideal.html   → 資產仍走 CDN，要網路（跟以前一樣，沒有變差）
啟動.bat / GitHub Pages   → 全離線，完全不碰網路          ← 決賽走這條
```

⚠️ **「GitHub Pages 全離線」這一格，2026-08-13 之前其實是不成立的** —— 那時線上根本沒有
vendor，`onerror` 一律退回 CDN。重跑部署把 vendor 推上去之後才真的成立。
（這段文字比事實早寫了三天，留這個註記提醒：寫「應該會怎樣」的時候要標清楚有沒有驗過。）

規則寫在 `js/mp-loader.js`，`vision.js` / `face-vision.js` 的 `locateFile` 只呼叫 `mpAsset()`。

⚠️ **版本號有三個地方**：`js/mp-loader.js`、`acunavi-ideal.html`、`vendor/下載.js`。
改一個就要三個一起改，JS 與 wasm 版本不匹配會偶發載入失敗、而且很難查。測試會擋。

⚠️ **要嘛整包都在、要嘛整包都不在。** 只留那三支 `.js` 而沒有 wasm 是最糟的狀態：
本機 `.js` 讀得到 → 不會觸發 `onerror` → 資產仍指本機 → 404。測試也會擋這件事。

⚠️ **驗到哪裡為止（2026-08-13 更新）：**

| 驗過的 | 怎麼驗的 |
|---|---|
| 本機 server 的 21 個檔 | 全 200、`.wasm` MIME 是 `application/wasm`、大小與 CDN 一致 |
| **線上 bisai 有 vendor** | `git ls-files vendor` 22 檔；blob 大小正確（wasm 6.29MB / tflite 5.48MB / data 4.33MB） |
| **線上抓得到** | `https://tenyeeming.github.io/bisai/vendor/mediapipe/hands/hands.js` 回傳真的 JS |
| **線上跑的是新版程式** | `js/vision.js` 線上版含 `warmUpHands` / `applyHandsOptions`，`results.image` 只剩註釋 |

🚨 **仍然沒驗的：從沒開瀏覽器實跑過攝像頭**，也**沒有量過任何 fps 數字**。
效能優化的實際改善幅度是估的，不是測的。這件事要人拿手機在現場做。

---

## 這個 demo 在做什麼

```
選症狀 ─┬▶ 01 選穴 ─▶ 02 認穴 ─▶ 03 定位 ─▶ 04 按摩 ─┬▶ 總結
 13 種  │   26 穴     定位說明    即時圓盤   右手 N 秒 │  各穴時間
（進入點）│             安全警語    角度閘門   ↓ 換手倒數│  總時長
        │             倒數自動翻   把手擺對   左手 N 秒 │
        └────────────── 自動接下一穴 ──────────────────┘
```

**首頁（選症狀）是進入點，不列入步驟軌** —— 人還沒開始做事，療程從選穴才算 01。
（2026-09-02 改；原本編成 01 症狀 … 05 按摩。）

底部四個分頁：**首頁**（上面這條流程）／**圖冊**（26 穴收集）／**設定**／**個人**（連續天數與統計）。

| 資料 | 數量 |
|---|---|
| 穴道總數 | 26（圖冊全都列） |
| 已實作定位公式 | 20（只有這些會被推薦） |
| 症狀 | 13（含 2 個急症） |
| 側緣穴（手背手心都能定位） | 10 |
| 傾角閘門 | 25° |

---

## 要改東西，開哪個檔

### 畫面：一頁一個檔

每個檔案上半是**行為**（`registerPage` 設定 + 函式），下半 `html:` 是**長相**（含這頁專屬的 CSS）。

| 想改的畫面 | 檔案 |
|---|---|
| 選症狀（進入點，不編號） | `pages/01-home.js` |
| 圖冊 → 單穴介紹（定位、主治、參考圖、收集狀態） | `pages/tab-gallery-info.js` |
| ① 推薦穴道、部位切換、勾選、急症 119 警示 | `pages/02-recommend.js` |
| ② 認穴（定位說明、安全警語、教學四步、**認穴倒數**） | `pages/03-acu-detail.js` |
| ③ 即時定位（取景框、信心圓盤開關） | `pages/04-camera.js` |
| ④ 雙手確認 + 左右手兩輪計時 + 換手倒數 + 齒輪選單 | `pages/05-massage.js` |
| 完成獎勵（小人徽章、連續天數）**自動模式下不停留** | `pages/06-complete.js` |
| **療程總結**（全部按完才出現：各穴時間、總時長） | `pages/08-summary.js` |
| **臉部即時定位**（穴道開關、WHO 說明、參數來源） | `pages/07-face.js` |
| 圖冊分頁 | `pages/tab-gallery.js` |
| 設定分頁 **目錄**（一個功能一列） | `pages/tab-settings.js` |
| 設定 › 語言 | `pages/settings-lang.js` |
| 設定 › 每日提醒（時間 + 每天要按什麼） | `pages/settings-notify.js` |
| 設定 › **療程節奏**（認穴倒數、手序、換手倒數） | `pages/settings-flow.js` |
| 設定 › 定位精度（嚴格模式） | `pages/settings-accuracy.js` |
| 設定 › 資料與紀錄（清除） | `pages/settings-data.js` |
| 個人分頁（連續天數、排行、等級分布） | `pages/tab-profile.js` |

### 其他

| 想改的東西 | 檔案 |
|---|---|
| 顏色、字體、圓角、深淺主題 | `css/tokens.css` |
| 外框、header、返回列、步驟軌、分頁列、按鈕、相機顯示窗、分段切換 | `css/shell.css` |
| 中英文字 | `js/i18n.js` |
| 存檔、連續天數規則、小人顏色、急症清單、**療程節奏預設值** | `js/state.js` |
| 部位分類（手部／手肘／臉部） | `js/regions.js` |
| 穴道小人的圖檔對照 | `js/minions.js` + `assets/minions/*.svg` |
| 穴道參考圖 | `js/acu-ref.js` + `assets/acu-ref/*.jpg` |
| 穴道教學影片 | `js/acu-video.js` + `assets/tutorial/*.mp4` |
| 切頁、返回列、步驟軌、分頁列 | `js/nav.js` |
| 相機、每幀偵測、畫穴位與圓盤、鏡像 | `js/vision.js` |
| **臉部**相機與偵測迴圈 | `js/face-vision.js` |
| **臉部**穴道資料（23 穴、WHO 定位、症狀對照） | `js/face-data.js` |
| **臉部**定位公式（臉座標系、PT/BT/PV） | `js/face-math.js` ⚠️ 改了要同步 `臉部/face_acu.py` |
| **臉部**按摩的對準判定（跨兩個模型） | `js/face-gate.js` —— 不是定位公式，不必同步 Python |
| header 與底部分頁列的結構與圖示 | `acunavi-ideal.html` |
| **穴道定位公式、症狀對照表、26 穴資料** | `js/acu-data.js`、`js/acu-math.js` ⚠️ 見〈已知風險〉 |

---

## 怎麼加一頁

1. 建 `pages/xx.js`：
   ```js
   registerPage('xx', {
     tab: 'home',
     step: 6, stepLabel: 'step-6',
     backTo: 'massage',
     onEnter: () => { /* 進頁渲染 */ },

     html: `
     <div id="page-xx" class="page">
       <style>/* 這頁專屬 CSS */</style>
       …
     </div>`,
   });
   ```
2. 到 `acunavi-ideal.html` 加一行 `<script src="pages/xx.js"></script>`。

**步驟軌、分頁歸屬、返回列都是從註冊資料自動長出來的，不用改 `nav.js`。**

`registerPage` 的欄位：

| 欄位 | 作用 |
|---|---|
| `html` | 這頁的 HTML（含專屬 `<style>`） |
| `tab` | 屬於底部哪個分頁：`home` / `gallery` / `settings` / `profile` |
| `step` + `stepLabel` | 填了就出現在步驟軌上，數字決定順序 |
| `backTo` + `backLabel` | 返回列退到哪頁、標什麼字；不填就不顯示返回鈕 |
| `actions` | 掛在返回列右邊的 HTML（按摩頁的齒輪選單就是走這個） |
| `hideTabbar` | 進這頁把分頁列收起來（定位頁、按摩頁用） |
| `keepsCamera` | 這頁需要相機；切到沒這旗標的頁面會自動關相機 |
| `onEnter` / `onLeave` / `onLanguage` | 進頁渲染 / 離頁收尾 / 切語言重繪 |

---

## 測試

```bash
npm install     # 只做一次，裝 jsdom
npm test
```

**七支，共 577 項**（2026-09-12）：
`check`（靜態，不需套件）／`e2e`（端對端流程）／`mirror`（鏡像繪製）／
`face`（臉部公式，拿 `臉部/標注結果/*.json` 的真實標注算 NME）／`angle-gate`（逐穴道角度閘門）／
`face-detail`（臉部白話資料與參考圖）／`guide`（引導箭頭）。
細節與「加測試時的注意事項」見 `tests/README.md`。

**動過任何一頁就跑一次。** 這個架構最容易踩的坑（多檔共用全域作用域、
i18n 漏翻、`fetch` 被加回來害得不能雙擊開）肉眼都看不出來，但測試抓得到。

### 🔴 動到**視覺**的話，上面那七支不夠 —— 還要跑多端檢查

```bash
python tests/browser_ui.py --output ../介面討論/<主題>_<日期>
```

真 Chromium（Playwright），**344 項**，七種尺寸 × 中英 × 深色，跑完逐頁截圖：
`phone 390×844`／`small 320×568`／`tablet 820×1180`／`desktop 1440×900`／
`landscape 844×390`／`large-text`（200% 字級）／`zoom-layout 720×450`。

> **用戶的核心訴求是「多設備使用」。** `npm test` 全綠不代表版面沒壞 ——
> 2026-09-19 的頁首深藍帶就是：418 項全過、手機上看起來完美，
> **桌面與平板上藍帶短了 272px、是階梯狀**。
>
> **截圖要自己打開看，不是跑完就算。** 同一天另一個 bug（「主訴」在藍底上幾乎看不見）
> 連 344 項也抓不到 —— 這支只驗「有沒有橫向捲軸／文字有沒有被裁」，不驗好不好看。

相機與模型在這支裡是替身，**不代表真實相機或定位準度驗過**。

---

## 穴道參考圖

`assets/acu-ref/` 一穴一張，來源是專案根目錄的 `自製穴道位置/`（手繪線稿 + 紅點）。
原圖是 800KB 的 PNG × 20 張 = 15MB，**縮到 560px 寬轉 JPEG 後剩 681KB**，
測試會擋住任何超過 200KB 的圖被放進來。

檔名沿用 `acu-data.js` 每個穴道的 `ref` 欄位，只是副檔名換成 `.jpg`。
**只有 20 穴有圖**，後補的 6 穴（少商／商陽／少衝／少澤／關衝／勞宮）沒有，
這時候會明講「尚無參考圖」——不拿別穴的圖頂替。

顯示在兩個地方：認穴頁（跟定位文字並排）、圖冊的單穴介紹頁（整塊寬度）。

⚠️ **`yangchi.jpg`（陽池穴）的紅點位置已知標錯**（`記錄控制/PROGRESS.md` 2026-07-14 記錄）。
這是參考圖本身畫錯，不是程式問題，**demo 前要重畫**，否則等於當著評審的面標錯位置。

---

## 穴道教學影片

`assets/tutorial/` 一穴一支 mp4，檔名沿用 `acu-data.js` 的 `ref` 欄位（副檔名換成 `.mp4`），
跟參考圖同一套命名。**目前只有合谷穴有片** —— 2026-09-20 起是**用戶交付的正式教學片**
（H.264 + AAC、1080×1920 直向、10.8 秒、13.7 MB），取代了 09-05 那支螢幕錄影佔位。
舊佔位片沒刪，移到 `D:\穴道按摩\教學影片\_舊佔位片_螢幕錄影_20260905.mp4`。
⚠️ 這支片**在版控裡也在線上**（`bisai` 是 public repo），內容是真人的手 —— 與 IRB 那條未解風險有關。

### 出現在哪兩個地方

| 位置 | 檔案 | 樣子 |
|---|---|---|
| 認穴頁的「看教學」區塊 | `pages/03-acu-detail.js` | 一顆「教學影片」按鈕，**排在四步文字說明前面** |
| 圖冊 → 單穴介紹 | `pages/tab-gallery-info.js` | 同一顆按鈕（`#info-video`），沒片的穴道整塊不畫 |

兩邊共用 `js/acu-video.js` 的 `acuVideoButton()` 與同一個全螢幕播放器，不是兩套。

### 白名單，不是「試著載載看」

`js/acu-video.js` 的 `ACU_VIDEO` 是白名單。⚠️ 本站要能用 `file://` 直接開，
那時 `fetch` 一律失敗，**探測不到檔案在不在** —— 只能靠這張表說實話。
表上沒有的穴道就只出文字步驟。
`tests/check.js` 雙向擋：表上有、檔案沒有 → 紅；檔案有、卻沒登記 → 也紅（等於白做）。

### 全螢幕播放器

⚠️ **不內嵌小框** —— 2026-09-05 試過，用戶說「不太好」（手機上那塊只有一指寬）。
點按鈕才開，覆蓋層貼在 `<body>` 上。

⚠️ **不靠 `requestFullscreen()`**：iOS Safari 的全螢幕只吃 `<video>` 自己
（`webkitEnterFullscreen`），對 `<div>` 無效 —— 靠 API 會變成「Android 全螢幕、iPhone 沒反應」。
覆蓋層是自己畫的，到哪都一樣。（仍會**試著**呼叫，成功的話 Android Chrome 再多吃掉網址列。）

| 操作 | 怎麼做 |
|---|---|
| 進度條 | 原生 `controls` 本來就有 |
| 跳轉 ±10 秒 | 影片左右兩顆圓鈕，或鍵盤 ←／→。步長 `SEEK_STEP = 10` |
| 退出 | 右上角 ✕、點影片以外的地方、Esc |

⚠️ **點影片本身不關**（不然快轉、調音量都會誤關），
⚠️ 跳轉鈕要 `e.stopPropagation()` —— 它跟「點外面關掉」共用同一層，不擋的話按一次就關了。

跳轉的邊界寫在純函式 `seekTarget(cur, delta, duration)` 裡（純函式才測得動）：
往前跳過頭回 `0`、往後跳過頭停在結尾、**長度還沒載好（`NaN`）時只夾下限**。
⚠️ 這個 10 秒要跟 App 的 `AcuVideo.SEEK_STEP_MS` 一致，兩邊測試各自釘住。

### 兩個連帶規則

⚠️ **展開教學會停掉認穴頁的自動翻頁倒數**，且收合後不重啟 —— 使用者自己按「開始定位」。
（原本倒數照走，會看到一半被翻走。）

⚠️ **離開頁面要 `closeTutorialVideo()`**：播放器貼在 `<body>` 上，切頁不會自己消失，
不關的話黑幕會蓋在下一頁上。認穴頁與圖冊介紹頁的 `onLeave` 都有做。

### 🚨 檔案大小是真問題

合谷那支正式片 13.7 MB（佔位片是 10.5 MB）。20 穴都放同等級的片 = **270 MB**，
GitHub Pages 與 APK 都吃不消。
真要每穴一支之前，先定壓縮規格（例如 720p／30 秒內／2 MB 上限）或改成線上載。

---

## 穴道小人（美術替換說明）

`assets/minions/` 底下一穴一個 SVG，共 26 個。**目前是程式產生的佔位圖，等你畫好直接覆蓋同名檔案即可**，
`js/minions.js` 和三個用到的頁面都不用改。

```
assets/minions/hegu.svg        合谷穴
assets/minions/yangchi.svg     陽池穴
…                              （對照表在 js/minions.js 的 MINION_SLUG）
```

| 項目 | 規格 |
|---|---|
| 格式 | SVG（PNG 也行，改副檔名要同步改 `minionSrc()`） |
| 畫布 | `viewBox="0 0 64 64"`，角色置中、四周留一點邊 |
| 背景 | **透明**。圖冊未解鎖時會套 `grayscale + brightness` 變成剪影，有底色會穿幫 |
| 顏色 | 建議沿用該穴道的代表色（`js/state.js` 的 `acuColor()`），跟清單圓點才一致 |
| 顯示位置 | 圖冊格（78% 寬）、完成頁徽章（84%）、個人頁排行（26px） |

圖檔載不到時底下會露出該穴道的代表色，所以少一兩個檔不會破版，只會變回色塊。
測試會檢查 26 個檔案齊全、沒有兩穴共用同一隻、也沒有多餘的孤兒檔。

⚠️ **不要放網路上抓的圖。** 這會進比賽 demo，著作權與須知第 66 條（虛偽不實）都是紅線；
而且外連圖片在 `file://` 或現場沒網路時會直接破圖。

---

## 臉部穴道（2026-08-09 新增）

**手部與臉部是兩套獨立的東西**，共用外殼與同一個 `<video>`，但模型不同
（`@mediapipe/hands` vs `@mediapipe/face_mesh`）、資料結構不同（手部用穴名、臉部用代碼如 `BL1`）、
公式不同。所以沒有併進 `ACUPOINTS`，而是另外一組 `face-*.js`。

| | 手部 | 臉部 |
|---|---|---|
| 模型 | Hands（21 點） | FaceMesh（478 點，`refineLandmarks:true` 才有虹膜） |
| 尺 | `cunPx`（每幀重算） | IPD 瞳距（座標系內建，同時吃掉 roll） |
| 資料 | `js/acu-data.js` | `js/face-data.js` |
| 公式 | `js/acu-math.js` | `js/face-math.js` |
| 相機迴圈 | `js/vision.js` | `js/face-vision.js` |
| 狀態 | `state.selectedAcupoints` | `state.selectedFace` |
| 相機旗標 | `keepsCamera` | `keepsFaceCamera` |

**入口**：選穴頁的「臉部」分頁勾選 → 跟手部一樣按「開始療程」走完整流程。
另有旁路「只看定位（不進療程）」→ `pages/07-face.js`。

⭐ **2026-09-04：臉部已經能走完整療程**（選穴 → 認穴 → 定位 → 按摩 → 完成）。
兩件事讓它成立：

1. **一層「療程項目」抽象**（`js/regions.js` 末尾）：各頁不問「這是不是臉部」，
   只問 `itemLabel` / `itemRounds` / `itemDetector` / `itemGated`。
   臉部正中穴 `itemRounds = 1`（沒有換手），所以按摩頁的輪次條顯示「臉部」而不是左右手。
2. **臉部項目一律排在手部之後**（`buildTreatmentList()`）。兩套模型共用同一個 `<video>`，
   切換要關掉相機串流再重開，是有感的黑畫面 —— 同類相鄰的話**整場只切一次**。

⚠️ **按摩頁是唯一同時跑兩個模型的地方**（`faceMode === 'massage'`）：
同一幀先 `await faceMesh.send()` 再 `await hands.send()`，因為閘門要看手。
**一定要一個 await 完再送下一個** —— wasm 是單執行緒佇列，同時送會讓兩邊
`onResults` 交錯回來，變成拿不同幀的手和臉配在一起。

**誠實顯示是刻意的**：沒實作定位的穴道會列出來但標「準備中」、不可勾選；
分頁上的數字只算已實作的，且「有穴道但都沒實作」會顯示「準備中」而不是 `0`
（`0` 會被誤讀成「這症狀沒有臉部穴道」，那是兩件不同的事）。
定位頁也把每條公式的**參數來源**印在畫面上（`WHO字面` 或 `fit(n=…)`）。

### 臉部的對準閘門：目前只看 2D

`js/face-gate.js`。手部的閘門靠「另一手指尖 vs 穴道」比距離，兩者出自
**同一個 HandLandmarker**；臉部跨兩個模型，而且 **2D 上「碰到印堂」跟
「手停在臉前 15cm 剛好擋住」看起來一模一樣**。

試過用 `R = 手的表觀大小 ÷ IPD` 判深度（近大遠小，不需要 z），
離線 96.5%、雙向跨場次 98.0% / 95.5% —— **但實機不行，關掉了**
（`FACE_DEPTH_CHECK = false`）。判定與常數整份留著，改 `true` 就生效。

**現在的判準只有一條**：指尖到穴位的 2D 距離 ÷ IPD，
進場 < 0.35（≈2.2cm）、退場 < 0.45（≈2.8cm）、掉出去後撐 400ms 才停錶。
退場比進場鬆是因為壓著時指尖會被自己的手擋住，landmark 會跳
（單一門檻會讓計時器一直閃「暫停」）。

🚨 **手浮在臉前面也會計時。這是知情的取捨。**
簡報／論文提到臉部時**不能宣稱有接觸驗證** —— 手部那邊才有，兩者不要混講。

實驗資料與教訓：`../../測試記錄/臉部閘門/README.md`。

---

⚠️ `js/face-math.js` 是 `臉部/face_acu.py` 的 JS 版，**兩邊必須一致**。
Python 那邊有 `臉部/驗證.py` 在算誤差，公式分家就沒得驗了。
`tests/face.js` 就是在守這件事：拿真實標注跑一遍，平均 NME 對不上就會紅。

---

## 產品規則（動之前先看這段）

這些不是隨手寫的，改動會直接影響比賽論述。

**「精度誠實」是核心主張** —— 系統會講出自己何時不可信。以下每條都在服務這句話：

| 規則 | 實作 | 為什麼 |
|---|---|---|
| 角度不佳**不畫點** | 傾角 > 25° 且嚴格模式開啟 → 擋下並說明 | 畫一個自己都不信的點，比不畫更糟。設定可關，關了會改標「僅供參考」而不是默默畫給你看 |
| 正反面不對**不畫點** | `isDorsalView` 比對穴道的 `side` | 手背穴畫在手心上是明確的錯 |
| 信心圓盤會**被壓扁** | 圓盤是 3D 基底投影的多邊形，越斜越扁 | 這是評審會記住的畫面：不確定性用形狀講，不用數字講 |
| 計時**只在指尖對準時前進** | `onTarget` 由每幀偵測更新，離開就暫停變灰 | 「確認真的按對」不是打卡 |
| 左右手**各一輪** | 設定的秒數是**單手**秒數，兩輪都完成才寫紀錄 | 同一個穴道左右手都有，只按一邊不算做完 |
| 自動接下去，但**不自動幫你擺手** | 倒數只負責翻頁；計時仍要 `onTarget` 才前進 | 能自動的只有翻頁。擺手這步自動不了，假裝自動就是假裝有在偵測 |
| **提早結束不寫紀錄** | 選單與確認框都明講不計入紀錄、圖冊、連續天數 | 讓人跳過還記一筆，等於自己拆自己的台 |
| **連續天數斷了顯示 0** | 最後一次是今天或昨天才算還活著 | 三個月沒開還顯示「連續 12 天」是假的 |
| 急症**擋話** | 選到「昏迷急救 / 中暑」跳紅框要求撥 119 | 穴位按壓不能取代急救，這條沒有商量餘地 |
| 只推薦**有公式的穴道** | 20 / 26 | 推一個按下去必定失敗的穴，等於當場示範系統不行 |
| 安全警語**強制顯示** | 合谷穴「懷孕忌按」等，來自 `ACUPOINT_DETAIL[].note` | — |

### 症狀覆蓋條（2026-09-14 新增，只有網頁有）

用戶提的問題：**「選了 a 和 b 兩個症狀，穴道全列在一起，萬一我不小心只勾到治 a 的怎麼辦？」**

`applySymptomRecommendation()` 把每個症狀的穴道**聯集成一份扁平清單**，「誰治誰」在那一步就丟了。

作法：標題下一排膠囊，一個症狀一顆，右邊數字 = **現在被幾個已勾的穴道覆蓋**（手部＋臉部一起算）。
點膠囊 = 只看那個症狀的穴道（部位徽章也跟著縮）。按「開始療程」時若有症狀是 0，
先出現一行提醒 ＋ 兩個選項：`看看有哪些` / `就這樣開始`。

| 決定 | 為什麼 |
|---|---|
| **不按症狀分區列清單** | 穴道會重疊（合谷同治感冒／腸胃／牙痛／頭痛），分區要嘛重複列、要嘛只能歸一區，都會讓人問「我勾的算哪一區」 |
| **不硬擋** | 今天只想處理 a 是合理的。而且 demo 當場被擋很難看 |
| **不自動勾** | 2026-09-09 定過「不用預設讓使用者自己選」，自動補等於把那個決定收回去 |
| **一穴都定位不了的症狀標「準備中」不標 0** | 那不是使用者的錯，標紅在罵他。也不進提醒 |
| **只選一個症狀時整條不出現** | 那種情況漏不掉，永遠顯示 1 的膠囊只是雜訊 |

程式：`pages/02-recommend.js` 的 `symptomCoverage()` / `renderCoverage()` / `tryStartTreatment()`，
臉部清單的篩選在 `pages/07-face.js` 的 `renderFaceList()`。測試在 `tests/e2e.js` 最末段（24 項）。
✅ 2026-09-14 已移植進 App（`比賽專區/AcuNavi/.../ui/pages/RecommendScreen.kt` ＋ `SessionViewModel.symptomCoverage()`）。
⚠️ 兩邊有一處**刻意不同**：網頁的覆蓋數把臉部勾選也算進去，App 只算手部 ——
因為 App 的選穴頁勾不了臉部穴道（臉部是另一條線，不併進手部療程）。

**部位分類**：手部 / 手肘 / 臉部。目前 26 穴全在手部，另兩個標「**準備中**」而不是「0」——
0 會被讀成「這症狀沒有臉部穴道」，但實情是這個部位整個還沒做，兩件事不能混。

要開臉部：`acu-data.js` 加穴道 → `regions.js` 的 `ACU_REGION` 標 `'face'` → 把 `REGIONS` 裡 face 的 `ready` 改 `true`。**選穴頁一行都不用改。**

---

## 療程節奏：自動跑完（2026-09-02 新增）

原本每換一次手、每換一個穴道都要手動點一下。問題是**手還舉在鏡頭前的時候，那一下最難點** ——
使用者得把手放下、點螢幕、再舉回去重新定位。所以改成可以全自動跑完，手動模式保留給想自己控節奏的人。

```
認穴（倒數 N 秒，按住畫面暫停）→ 定位 → 按摩（第一隻手 N 秒）
   → 換手倒數 N 秒自動接上 → 按摩（第二隻手 N 秒）
   → 自動接下一穴 …→ 全部按完 → 總結頁
```

⚠️ **能自動的只有翻頁。** 倒數結束只是把認穴頁翻過去，翻過去之後仍然要使用者
把手擺到鏡頭前、`onTarget` 判定對準，計時才會走。這條界線不能模糊掉。

### 五個設定，按「來不來得及改」分兩處放

| 放哪 | 項目 | 預設 | 為什麼放這 |
|---|---|---|---|
| **設定 › 療程節奏** | 認穴倒數 `readySec` | 5s（可設 0＝跳過） | 療程開始後才想改就來不及了 |
| | 手序 `handOrder` | `right`（先右後左） | 同上 |
| | 換手倒數 `switchSec` | 5s | 同上 |
| **按摩頁** 齒輪選單 | 換穴模式 `autoAdvance` | 自動 | 按到一半會想改，改了立刻有感 |
| **按摩頁** 滑桿 | 單手秒數 `pressSec` | 30s | 同上 |

預設值在 `js/state.js` 的 `FLOW_DEFAULT`，存 `localStorage.flowSettings`。

⚠️ **`readySec = 0`（跳過認穴）有代價**：MediaPipe（~16MB）原本是趁使用者讀定位說明那幾秒
在背景載完的（`warmUpHands()`），跳過會讓定位頁開頭卡一下。這是使用者自己選的，不擋。

⚠️ **單手秒數 × 2 × 穴道數 = 總時長。** 30 秒 × 左右 × 3 穴 = 3 分鐘；
60 秒的話就是 6 分鐘 —— 決賽現場 demo 前記得先調短。

### 自動模式下，單穴完成頁不停留

小人獎勵改到**總結頁**一次看完，中間每一穴都停一次會把流暢感打斷。
手動模式（齒輪可切）維持原樣停在完成頁。
**長期紀錄、圖冊、連續天數的寫入邏輯完全沒動** —— 那仍然由 `completeMassage()` 負責。

---

## 全螢幕按摩（2026-08-13 新增）

按下「開始按摩」之後，**取景框會跳出手機外框、鋪滿整個瀏覽器視窗**，
計時與讀數條疊在畫面下緣，本輪是哪隻手標在左上角。

理由：那一刻滑桿和開始鈕都已經鎖住，底下整塊面板是死的；
而使用者真正要盯的只剩兩件事 —— **穴道圓盤**和**指尖有沒有對準**。
投影出來給評審看時也是這個畫面最有效。

| 進 / 出 | 觸發 |
|---|---|
| 進 | 按「開始按摩」（`startMassage()`） |
| 出 | 一輪計時歸零、提早結束、離開這一頁（都會經過 `stopMassageTimer()`） |
| 出 | 按 HUD 右上角「⤡ 縮小」或 **Esc** |
| 再進 | 縮小後計時**還在跑**，面板上會出現「⤢ 放大顯示」 |

**實作重點：計時器與讀數條是「搬」進 HUD 的，不是複製一份。**
`enterMassageFullscreen()` 記下原本的 `parent` / `nextSibling`，退出時放回去。
複製一份就會有兩個計時器要同步；而搬 DOM 完全不影響 `updateTimerDisplay()`
和 `setGate()`，因為它們都是靠 `id` 找元件。

> ⚠️ 全螢幕時齒輪選單（切換鏡頭 / 提早結束）在返回列上，會被蓋住。
> 這是刻意的——要用那些功能就先「縮小」。e2e 測試也照這個順序走。

樣式全部寫在 `pages/05-massage.js` 自己的 `<style>` 裡（只有這一頁用），
開關是 `document.body` 上的 `.massage-fs` class。

---

## 設定：一個功能一頁（2026-08-13 改版）

**設定分頁本身只是目錄**，一列一個功能、右邊灰字寫「現在設成什麼」，點進去才調整——
跟 LINE 的設定一樣。以前四個開關全擠在同一頁，每個開關旁邊塞不下解釋，
而「嚴格模式」正好是全站最需要解釋的一個。

```
設定
 一般 ┬─ 語言          中文          ›
     └─ 顯示          中            ›   ← 2026-09-21 補（字體大小）
 療程 ┬─ 療程節奏      5s · 右手 · 30s ›
     ├─ 我的流程      尚未建立       ›
     └─ 每日提醒      20:00 · 合谷穴 ›
 資料 ── 資料與紀錄                  ›
```

> ⭐ **2026-09-21 起這張圖＝App 的設定目錄**（`SettingsScreen.kt`），分組、順序、內容都一樣。
> 在那之前網頁少一頁（顯示）、多一個入口（定位精度）、「每日提醒」還掛在一般組。
> ⚠️ **「定位精度」的入口已經拿掉** —— App 2026-09-09 就刪了（行為固定嚴格，用戶指定）。
> 頁面檔 `pages/settings-accuracy.js` 與 `strictGate` 都留著沒動，只是目錄進不去。
> ⚠️ App 的「顯示」頁還有一個「顯示手部節點」開關，**網頁沒有** —— 網頁的 `js/vision.js`
> 本來就不畫那 21 個白點，沒有東西可以關。

子頁一律 `tab: 'settings'` + `backTo: 'settings'`，所以返回列與分頁列自己會長對。
**新增一個設定項就是新增一頁**，不要往現有子頁塞第二個功能。

### 每日提醒能設什麼

時間之外，還能指定**今天要按什麼**，三選一：

| 模式 | 設什麼 | 點提醒之後落在哪 |
|---|---|---|
| 不指定 | — | 首頁 |
| 依症狀 | 勾幾個症狀 | 幫你把症狀勾好 → 推薦穴道頁 |
| 直接選穴道 | 勾幾個穴道（只列已實作定位的 20 穴） | 排成今天的療程 → 認穴頁 |

勾選存的是**名字**不是索引（索引會被 `SYMPTOM_MAP` / `ACUPOINTS` 的增刪弄歪）。
**落地都停在「還沒開始按」那一步**——幫你把東西準備好，但「開始」還是使用者自己按。

兩條刻意的界線：

- **急症（昏迷急救／中暑）不列進提醒清單**，並在清單下方寫明為什麼。
  排程「明天晚上八點來按中暑」這件事本身就是錯的訊息，跟選穴頁的 119 擋話是同一條規則。
- **提醒只在網頁開著時會響**，畫面上直接寫明「這一版沒有背景排程，我們不假裝有」。
  要真正的背景提醒得有 Service Worker（`file://` 下不能用）或做成原生 App，兩者都不在這一版。
  ⚠️ 改版前這裡是**只有一個開關、什麼都不會發生**的假功能，決賽詢答被問到會直接破功。

## 幾個刻意的技術決定

**HTML 寫在 `.js` 裡，不是獨立的 `.html` 檔。**
因為要能雙擊開。`file://` 是匿名來源，在這個來源下 `fetch()` 讀鄰居檔案一律被 CORS 擋掉，
但 `<script src>` 不會 —— 所以片段搭 script 的順風車進來。
（測試會擋住任何 `fetch` / `XMLHttpRequest` 重新出現在程式碼裡。）

**各頁的 CSS 寫在自己的 `html` 裡。**
用 `innerHTML` 插入的 `<style>` 瀏覽器會照常套用（只有 `<script>` 不會執行）。
只有兩頁以上共用的樣式才放 `css/shell.css`。

**前鏡頭鏡像，而且只鏡射「畫面」。**
`facingMode === 'user'` 才翻（後鏡頭是「看別人」，鏡了反而不對）。
landmark 座標一行都沒動 —— 正反面判定、傾角、定位公式全吃原始值。分三種畫法：

| 畫什麼 | 怎麼畫 | 為什麼 |
|---|---|---|
| 影像、信心圓盤 | canvas `transform` 翻 | 圓盤是 3D 基底投影出來的多邊形，整組頂點都得翻，只翻中心點會變形 |
| 穴位點 + 穴名 | 不用 transform，自己算 `W - x` | 用 transform 的話穴名會變成反字 |
| 指尖標記、引導箭頭 | 同上 | 距離判定仍在原始座標算，不受影響。⚠️ **兩端都要先 `mx()` 過再算方向**，混用會讓箭頭指反邊 |

**定位頁與按摩頁共用同一個 MediaPipe 實例**（`js/vision.js`），定位→按摩不重啟相機、不重載 wasm。
所以這是單頁式載入，不是九個獨立網址。

**定位頁與按摩頁會收起底部分頁列。** 取景框需要垂直空間，而且按摩時手在鏡頭前，誤觸切頁會直接中斷計時。

**切語言只重繪「當前這頁」。** 重繪別頁會把使用者勾到一半的穴道清掉。

---

## ⚡ 效能：動之前先看這段（2026-08-13）

完整診斷在 `比賽專區/文件/網頁卡頓診斷與優化_20260813.md`。這裡只放「改的人必須知道」的四件事。

**① 卡的不是公式，是推論與影像搬運。**
整個 `acu-math.js` 每幀成本估不到推論的 1%（`computeAcuConfidence` 約 50 個浮點運算、`_convexHull` 21 點、圓盤 36 邊形）。
→ **任何「為了流暢簡化公式／關掉圓盤／降精度」的做法都是錯的**：省不到東西，只會拆掉「精度誠實」這個主張。

**② 這幾處是效能改動，不要「順手還原」：**

| 位置 | 現在的樣子 | 為什麼 |
|---|---|---|
| `vision.js` `applyHandsOptions()` | 定位頁 `maxNumHands:1`、按摩頁 `2` | 定位頁本來就只用 `best` 那一隻手，第二隻的推論算完就丟 |
| `vision.js` `drawImage(video, ...)` | **刻意不用 `results.image`** | 那是 MediaPipe 內部的 WebGL canvas，畫進 2D canvas 會強迫 GPU→CPU 同步讀回 |
| `vision.js` canvas 上限 640 | **等比**縮小 | 640×480 是 getUserMedia 的 ideal 不是 exact，手機常回 720p；而 MediaPipe 內部一律縮到 ~224×224 推論 |
| `acu-math.js` `drawAcupoint` | 雙圈光暈，**不是 `shadowBlur`** | canvas2d 的 shadow blur 在行動端常走軟體路徑，是最貴的單一操作 |
| `shell.css` `#hidden-video` | 畫面外 1px + `opacity:0`，**不是 `display:none`** | 部分 iOS Safari 對 `display:none` 的 video 不解碼 → `drawImage` 拿到全黑幀 |

**🚨 ③ canvas 尺寸只能等比縮放，長寬比一格都不能動。**
`acu-math.js:215-218` 註釋③：`_cv()` 是 x,z 乘 W 而 y 乘 H。等比縮放時三軸同倍數 → 法向量方向不變 → `conf`/`angleDeg` 完全相同；
**長寬比一改（例如硬塞成 4:3）法向量就會歪，閘門三的判定跟著錯。** 這是「偷偷改精度」，不是效能調校。

**④ `modelComplexity` 維持 `1`，不要改成 `0`。**
lite 模型快 1.5–2 倍，但 landmark 誤差直接進 v35 公式，而本專案閾值是 **2mm**。
要做就做成 `pages/settings-accuracy.js` 裡的明示選項（畫面標「省電模式，定位精度較低」），**不能為了 demo 順而偷偷開**。

**預熱**：`03-acu-detail.js` 進頁時呼叫 `warmUpHands()`，趁使用者讀定位說明時把 MediaPipe（~16MB）在背景載好。
它只載模型不開相機（`onHandsResults` 開頭 `camRunning=false` 就 return）。**加新頁時，如果那頁的下一步是定位頁，記得也呼叫它。**

---

## ⚠️ 已知風險

**`file://` 下的攝像頭還沒實機驗證過。**
理由上應該可以：`getUserMedia` 在 `file://` 算安全來源，MediaPipe 的 wasm 從 jsdelivr 抓（CDN 有開 CORS）。
專案 `CLAUDE.md` 記過「MediaPipe 網頁原型不能用 `file://` 開（WebGL 跨域擋圖）」——
那條講的是**讀本機照片檔**（照片模式），本 demo 只用攝像頭，應該不受影響。
**請實際雙擊測一次攝像頭**；不行就用 `啟動.bat`。

~~**MediaPipe 走 CDN，要連網。**~~
**2026-08-13 做完了**，見〈MediaPipe 離線化〉。**但只有用 `啟動.bat` 開才是離線的**；
雙擊開的那條路徑因為 `file://` 擋 `fetch`，資產仍走 CDN。決賽用 `啟動.bat`。

**左右手兩輪沒有自動驗手。**
順序寫死「先左後右」，系統不會檢查你真的換手了。技術上可行（MediaPipe 有 handedness），
但專案記過 handedness 鏡像陷阱（`is_dorsal` 在鏡像攝像頭 vs 靜態照片會相反），
沒實機驗證前硬擋有機率在 demo 當場誤判把人卡住。

**這是快照，不是連結。**
`js/acu-data.js` 與 `js/acu-math.js` 是定位公式的複本，改這裡不會回饋到 `网页版_2d/`，反之亦然。
正式版更新公式後要手動同步過來，否則 demo 用的是舊公式。

**陽池穴的參考圖紅點標錯**，見上面〈穴道參考圖〉。

沒複製 `assets/acu-ref/`（16MB 參考圖），本版只用文字定位說明。

## 2026-09-16（Codex）— 現況檢查與收工待辦

用戶要求查看網站後，指定暫停、更新 README 收工。本次沒有修改網站程式、定位公式或標注，沒有部署。

### 本次實際驗證

執行 `node tests/run.js`：**603 項通過、1 項失敗**。check 34、e2e 433、mirror 11、face 60、angle-gate 35、face-detail 10、guide 20 項通過。
失敗項：**素髎 GV25_M NME 9.65%，超過測試上限 8%**，交由 Claude 覆核公式與測試依據。不得將目前狀態記為全部測試通過。
主要操作流程的 433 項 jsdom 測試全過；MediaPipe 使用替身，這不代表相機或手機實測通過。

### 下次續接

- 手機查看首頁：七列症狀格最低共 592px，下一步在清單後方；確認捲動成本，再決定是否調整按鈕位置。格子高度來自既有用戶要求，本次未改。
- 覆核位置判定文案：face-vision.js 仍有「按壓中」「無法驗證有沒有按對」，首頁與推薦也有疾病／急救選項及「療程處理症狀」的措辭。系統只判 2D 位置，文案須符合專案紅線。
- 自動流程獎勵呈現：06-complete.js 跳過完成頁，08-summary.js 目前只顯示穴名與時長，未顯示註解所說的集中小人獎勵；收集資料仍有寫入。待用戶決定是否補呈現。
- Claude 覆核素髎失敗；定位改良仍凍結，本次檢查不授權調常數。
- 未驗項：真實瀏覽器版面、手機相機、離線使用、線上部署與本機版本是否一致。

依據為本機原始碼及本次測試輸出，不是新增照片或量測。完整交接：`D:\穴道按摩\AI聊天區\交接_網站現況檢查_20260916_Codex.md`。

協作已採用 `D:\穴道按摩\AI聊天區\交接目錄.md`：每次行動前查看給自己的待覆核項目，有才開交接文件；仍須讀現況與對話。Codex 固定入口已寫入 `D:\穴道按摩\AGENTS.md`。

## 2026-09-16（Codex）— App 對齊與多端適配

本輪依 App 的流程與視覺完成網頁介面調整，範圍是 `demo網站/` 的流程、介面與測試，不動定位公式：

- 共用外殼加入響應式樣式：手機直排、平板／桌面使用較寬版面；底部分頁列改為懸浮膠囊，選中項目展寬。
- 步驟軌保留給輔助科技，畫面改顯示當前步驟、進度數字與進度條。
- 首頁最多選五個症狀，角色站位隨選擇數更新；聲明放在下一步按鈕前。選項與按鈕可換行，支援窄螢幕、橫向與大字體。
- 選穴頁移除逐穴秒數展開入口；秒數仍可在按摩頁與我的流程調整。詳情面板在手機改為底部抽屜，支援 Esc、Tab 焦點循環與關閉後焦點復原。
- 按摩開始不再強制全螢幕；由使用者按「放大顯示」進入。新增明確的暫停／繼續狀態，暫停保留剩餘時間，切換頁面會退出放大畫面。
- 總結頁加入五個角色、當次獎勵與連續天數；重繪或切語言不會重複記帳。
- 修正位置判定文案，避免宣稱系統能驗證力道或接觸深度。

驗證：`node tests/check.js`、`node tests/e2e.js` 全過；新增 `tests/browser_ui.py`，以 Playwright Chromium 在 390×844、320×568、820×1180、1440×900、844×390、200% 字體等尺寸檢查首頁、選穴、認穴、相機、按摩、總結、設定與深色主題。共用相機模型使用替身，因此不代表真實相機或定位準度已驗證。截圖與 JSON 報告在 `比賽專區/介面討論/網頁多端驗證_20260916/`。

既有定位測試仍有一項已知失敗：素髎 GV25_M NME 9.65%，超過 8% 門檻；本輪沒有修改公式或常數。

---

## 2026-09-19（Claude）— 這份網站改由 Claude 維護 ＋ 補上 App 的外觀四項

### 分工改了

用戶：「**我決定網頁還是交給你做**」。09-16 那版「Claude 管 App／Codex 管網頁」結束 ——
**這個資料夾現在由 Claude 動手**，Codex 轉為覆核、要動代碼先問用戶。
⚠️ 上面兩節 Codex 做的東西**全部留著繼續用**，換人不等於推倒；
`responsive.css` 的多端適配與 `tests/browser_ui.py` 這次都是直接沿用。

### 補上 App 的外觀四項（全 CSS 層，零邏輯改動）

先完整比對，發現兩邊**不是全面分岔** —— 09-16 那批已經把膠囊分頁列、步驟進度條、
首頁角色站位、手掌浮水印都對上了。真正沒過來的只有：

| # | 改了什麼 | 檔案 | 依據的 App 原始碼 |
|---|---|---|---|
| ① | **頁首深藍帶 `#14487D`**（刻度條 → 標題列 → 首頁 lede） | `css/tokens.css`（`--hero`／`--hero-ink`／`--hero-ink-soft`）、`css/shell.css`（header 底色＋白底線、wordmark）、`pages/01-home.js`（包一層 `.hero-band`）、`css/responsive.css`（出血與字色） | `Color.kt` 的 `HeroL`、`AcuScaffold.kt` 的 `Header()`、`HomeScreen.kt:137` |
| ② | **圓角分級**：`--r` 3px → **8px**，加 `--r-chip` 4／`--r-card` 12／`--r-sheet` 28／`--r-pill` 999 | `css/tokens.css` ＋ `.notice`／`.gear-btn`／`.back`／`.sheet` | `Theme.kt` 的 `AcuShapes` |
| ③ | **按鈕膠囊**（`.seg` 不跟，App 的 SegRow 走 small） | `css/shell.css` 的 `.btn` | `AcuButtonShape = 22dp` |
| ④ | 圖冊格與個人頁統計卡**補微陰影**（深色清成 none） | `css/responsive.css` | `cardElevation`，全 App 只有四處 |

> `--r` 直接改 8px 而不是逐處分派：grep 全 App，`shapes.small` 約 25 處、`medium` 只 2 處。

**刻意沒做**：穴道數量維持 **20 穴**（用戶拍板不跟 App 的 13 穴對齊 —— 那是內容不是界面）；
「昏迷急救」網頁端殘留（**選單仍是 13，App 是 12**）這批不處理；
App 的「顯示」設定子頁（字級、隱藏骨架節點）沒補；
個人頁**沒有**拆成 App 那樣的一格一張獨立卡片（那是版面改動）。

### 🔴 多端檢查抓到兩個 bug（手機上都看不出來）

用戶提醒「**我的核心是多設備使用**」之後補跑 `tests/browser_ui.py`：

1. **桌面／平板藍帶短 272px，成階梯。** `≥760px` 時首頁是兩欄 grid，`.hero-band` 只有左欄寬。
   → 右欄寬與欄距抽成 `--home-aside`／`--home-gap`，藍帶右側多出血這兩段；
   並限制 `.hero-band > *` 的 max-width，否則 `.home-heading` 的 space-between
   會把小人推到畫面最右邊。
2. **「主訴」在藍底上幾乎看不見。** `.device .hero-band .eyebrow` (0,3,0) 輸給既有的
   `.device :is(.eyebrow, .meter .label, .sheet .field .k, .cover-chip .n)` **(0,4,0)** ——
   **`:is()` 的特異性取括號內最強的那一項**。→ 加 `#page-home` 變 (1,3,0)。

**驗證**：`browser_ui.py` 344 項全過、零 pageerror；另量四種尺寸下 header 與藍帶的邊界，
**左右對齊、垂直間隙 0**；`node tests/run.js` 與改動前逐項相同（face 仍是素髎那支已知紅）。
截圖 35 張 ＋ report.json 在 `比賽專區/介面討論/網頁對齊App外觀_20260919/`。

**未驗**：真手機、與 App 並排比對、深色實機。
**未部署**：`.deploy-bisai` 停在 2026-09-15，09-17 那批加這批都還沒上線。

---

## 2026-09-20（Claude）— 桌面版型重設計 ＋ 即時數據面板 ＋ 上線

### 一、線上追平了（09-15 → 09-20）

上一節結尾寫的「未部署」已解除。`b24d0cc..8034a09`，26 個檔案，
**https://tenyeeming.github.io/bisai/** 已驗證生效（影片 13,693,083 bytes、
`css/responsive.css` 200、`assets/cast/*` 200）。09-16 的多端適配、09-17 的症狀上限 3、
09-19 的外觀四項，全部是這次才真的上線的。

🚨 **`比賽專區\發布\部署.ps1` 會在 commit 之前中斷，還沒修。**
腳本 `$ErrorActionPreference = "Stop"`，而 `git add -A` 會把
`warning: ... LF will be replaced by CRLF` 寫到 **stderr** ⇒ PowerShell 5.1 包成
`NativeCommandError` ⇒ **robocopy 同步做完了，commit 與 push 沒跑**。
症狀是「跑了但線上沒變」。這次是手動接完後兩步。
→ 修法：腳本裡的 git 呼叫包 `try { … } catch {}`。（CLAUDE.md 雷區「PS 對 native stderr」的變種。）

### 二、桌面版型重設計：左側固定導覽欄

用戶：「網頁版本的界面需要重新設計」→「**就版型沒有適配電腦界面**」，方向定為**比賽評審導向**。

**改動前的實際問題**（先跑 `browser_ui.py` 截 1440×900 的圖確認，不是憑印象）：

| # | 問題 |
|---|---|
| ① | `.device` 限 1120px 置中 ⇒ 左右各一條 160px 灰邊，整頁像「一個殼」 |
| ② | `.stack` 又限 760px 置中 ⇒ 內容被夾**第二次**，選穴頁卡片只佔中間一半 |
| ③ | 底部膠囊分頁列是手機的東西，原封不動擺在桌面正下方 ⇒ 一眼就是手機截圖 |
| ④ | 高度沒用滿：按摩頁相機下方、首頁右欄上方都是大片空白 |

> ⚠️ 09-16／09-19 那兩批**有**做 `@media (min-width:760px)` 的兩欄，但只針對首頁／定位／按摩，
> 外層兩道寬度夾心沒動 —— **「有適配」與「看起來像桌面應用」是兩回事。**

**改法**（`css/responsive.css` 的 `@media (min-width: 1024px)`）：

- `.device` 從 flex column 改成 **grid ＋ 具名區域**，重排成「左欄導覽 ＋ 右欄內容」
- 分頁列直排成左欄；**淺色底接 `--hero`**（跟品牌帶連成一條藍）、**深色回 `--tab-bg`**
- 拿掉 `.stack` 的 760px 夾心（改 1180px 靠左）；超寬 ≥1600px 再整體置中
- 定位／按摩改 2fr : 1fr；圖冊改 `auto-fill minmax(150px,1fr)`
- 相機 `.viewport` 限寬，讓 4/3 的高度回到視窗內

⭐ **DOM 結構一個字都沒動** ⇒ 418 項 e2e 完全不受影響。
`#hidden-video`（fixed）、`.steprail`（absolute）、`.step-progress[hidden]` 三者都不佔 grid 格。

#### 🔴 做的時候被抓到的兩件事

1. **`tests/browser_ui.py` 當場擋下**：左欄第一版寫 `background: var(--hero)`，
   但 `--hero` 是固定 `#14487D`、深色模式下不會變 ⇒ `desktop: dark theme navigation` **FAIL**。
   → 淺色 `--hero`、深色 `--tab-bg`，兩邊都成立。**這條斷言救了一次。**
2. **流程頁（認穴／定位／按摩／完成）分頁列是藏起來的**，左欄就成一條 232px 白帶。
   而它是用 **inline style** `display:none` 藏的、CSS 嗅不到 ——
   所以 `js/nav.js` 的 `updateTabbar` 多掛一個 `.device.in-flow`（**本批唯一的 JS 改動，3 行**），
   CSS 據此收掉左欄、走全寬單欄。
   ⚠️ 刻意不用 `[style*="none"]` 選擇器：那綁死字串序列化，改成 hidden 屬性就會靜靜失效。

### 三、大螢幕的空白改放「即時數據面板」

用戶追問：「**電腦屏幕太大沒辦法適配**，你覺得怎麼辦」。

**判斷：這不是 CSS 適配不了，是內容量的問題。** 首頁只有 11 個症狀格、選穴頁 11 張卡，
手機上「一屏剛好」，1440 螢幕上怎麼排都會剩。硬填只有兩條路且都不好 ——
卡片撐大（變簡報）、或多欄擠滿（掃視更難）。

→ **不追著填滿跑，把多出來的空間拿去顯示系統本來就在算、手機上沒地方放的數字。**

| 位置 | 內容 |
|---|---|
| 定位頁右欄（`#camera-stats`） | 定位信心 · 整手傾角 · 皮膚偏角/上限 |
| 按摩頁右欄（`#massage-stats`） | **指尖離穴道 ?? mm**（放大字，對準綠／沒對準紅）＋ 上面三項 |
| 首頁右欄（`.intro-card`） | 手部 20 穴 · 臉部 23 穴 · 測試閾值 2 mm · 全部在裝置上運算 |

⭐ **面板裡一個數字都不是新算的。** 全部取自 `js/vision.js` **同一幀的既有判定**：
`best.info.conf`、`computeHandTiltDeg`、`best.gate.angleDeg/limitDeg`、
按壓段的 `minD`（用 `CUN_MM = 17.66` 換成 mm）。
⇒ 面板與畫面必然一致，不會出現「圈說對準了、數字說差 5mm」。
算不出來一律顯示 `—`，**不拿 0 充數**。

**`renderLiveStats()` 掛在 `setGate()` 裡**：主迴圈有七、八個 return 點，
逐個補 render 遲早會漏掉一個，而漏掉的症狀是「數字卡在上一幀」——很難發現。
提示列每幀都要更新，搭它的便車最省事也最不會漏。

#### ⚠️ 對外措辭：「校準誤差 ≤2mm」是錯的，已改

首頁概要卡第一版我寫「校準誤差 ≤2 mm」。**2mm 是測試的通過閾值**
（沿革 5mm → 4mm → 2mm，見 `記錄控制/`），校準目標才是 ~1mm，**不是實際達成的誤差**。
→ 已改成「**測試閾值 2 mm**」。
這正是 Codex 2026-09-16 覆核時抓過的那類問題，寫對外文字時要一直記著。

### 四、驗證與缺口

- ✅ `tests/browser_ui.py`：**344 項全過、零 pageerror**（七尺寸 × 中英 × 深色）
- ✅ `node tests/run.js`：check 34／**e2e 418**／mirror 11／angle-gate 35／face-detail 10／guide 20 全綠；
  face 仍是素髎那支已知紅（與本批無關）
- ✅ 截圖自己開來看過（不是只看測試綠）：`比賽專區/介面討論/桌面版型重設計_20260920/`
  的 `before/` 與 `after-final/`
- ❌ **面板的數字沒有在真相機下看過** —— `browser_ui.py` 用的是相機替身，截圖裡四項全是 `—`。
  **數值對不對、跳動快不快、好不好讀，完全沒驗。這是本批最大的缺口。**
- ❌ 沒有真桌面瀏覽器實測（Playwright Chromium ≠ 使用者的 Chrome/Edge）
- ❌ **二、三兩節還沒部署** —— 線上是 09-20 上午那版（只含第一節）

### 五、幾個我自己不確定的地方

1. **1024px 這個斷點是我定的**。760–1023（平板橫放、小筆電）仍走舊的「兩欄 ＋ 底部膠囊」。
2. **`.stack` 靠左不置中**（1600 以上才置中）。理由是桌面應用的內容貼著導覽欄排。
3. **`.in-flow` 把左欄整個收掉**是我的判斷。另一種做法是左欄保留、改顯示步驟進度 ——
   對評審可能更好（看得到「現在第幾步」），但那要動 DOM。
4. **相機限高的 `calc((100svh - 300px) * 4 / 3)`，那個 300px 是估的**，字級一改就不準，
   而且 `browser_ui.py` 只驗橫向捲動與裁字、**不驗有沒有超出視窗高度** —— 這條沒有測試擋。

---

## 2026-09-20（Claude，續）— 刪「昏迷急救」＋ 手臉病症合併（批 36、37）

> 這兩批都是**資料口徑對齊**，不是新功能。來源都是使用者交付的
> `D:\穴道按摩\資料存放區\穴道資料庫_手臉合併_20260917_修訂版.xlsx`。

### 一、批 36：刪掉「昏迷急救」與急症擋話

09-17 使用者就拍板過（「昏迷急救刪除吧，中暑可以留著」），但**只有 App 做了，網頁一直沒跟上**，
選單項數一度是 App 12 vs 網頁 13。這批補齊。

| 檔案 | 動了什麼 |
|---|---|
| `js/acu-data.js` | `SYMPTOM_MAP` 刪「昏迷急救」→ 13 → 12；中暑保留，但降為**一般症狀** |
| `js/face-data.js` | 刪 `'昏迷急救': ['GV26']`；GV26 的 `func` 留空、按法與說明逐字對齊 App |
| `js/state.js` | `EMERGENCY_SYMPTOMS` 常數整個刪除 |
| `pages/02-recommend.js` | 119 紅框（函式＋呼叫＋`<div id="emergency-warn">`）全移除；臉部詳情改成 **func 空就不畫標題** |
| `pages/settings-notify.js` | 提醒清單的急症排除、那句 119 說明拿掉 |
| `js/i18n.js` | 刪中英 `settings-notify-emergency`、刪昏迷急救英譯、`Heatstroke (Emergency)` → `Heatstroke` |

⭐ 對外口徑改成「**本系統定位為日常自我保健，不作為急救、診斷或治療工具**」。
⚠️ **水溝穴 GV26 因此沒有任何症狀歸屬**，只剩圖冊看得到。這是刻意的，歸屬待使用者決定 —— **不要自行補回去**。

### 二、批 37：手臉病症合併，症狀表 12 → 16

使用者問「臉部的病症分類加入了嗎」→ 查出來**只接了一半**：`FACE_SYMPTOM_MAP` 借用手部症狀名當 key，
只掛得上 6 項，**臉部專屬 4 項完全沒進去**，而且已接的 6 項跟資料庫對不上。

使用者當場拍板：**① 以 xlsx 為準（不取聯集） ② 症狀名照原文 ③ 空分頁不特別處理**。

| 症狀 | 舊有（臉部） | 資料庫 | 處置 |
|---|---|---|---|
| 緩解目痛 | 10 穴 | 7 | 砍成 7（移除絲竹空／球後／太陽） |
| 緩解頭痛 | 5 穴 | 3 | 重建（移除陽白／攢竹／上關，補回絲竹空） |
| 緩解牙痛 | 5 穴 | 4 | 重建（移除地倉／巨髎，補回顴髎） |
| 緩解感冒症狀 | 4 穴 | **空欄** | 整項移除 |
| 緩解耳鳴 | 2 穴 | 2 | 本來就一致 |
| 改善失眠 | 2 穴 | 1 | 砍成 1 |
| **美容／鼻子不適／顏面神經麻痺／口腔衛生** | 不存在 | 10／4／3／1 | **新增** |

⭐ **顴髎 SI18** 原本零歸屬，靠牙痛／美容／顏面神經麻痺接回來了。
⚠️ **球後 EX-HN7 反而變成零歸屬**（資料庫的目痛沒列它），只剩圖冊看得到 —— 待使用者決定要不要補。
⚠️ 被砍掉的那些舊對應**查不到來源**，已請 Codex 協查；若其實有根據，這批要回頭補。

### 🚨 新測試當場抓到一個真 bug（已修）

臉部專屬症狀在手部 0 穴，而 `initRecommendList()` 寫死 `currentRegion = 'hand'`
⇒ 選「美容」進選穴頁會看到**一片空清單**。
→ 新增 `defaultRegion()`：手部推薦不到任何穴、而臉部有可定位的，就直接落在臉部。
兩邊都有（或都沒有）維持落在手部。**App 端同一套規則**（`SessionViewModel.defaultRegion()`）。

### 驗證

- ✅ `node tests/run.js`：**e2e 417 → 439**（新增 19 項守門 ＋ 4 項分頁行為），其餘全綠。
  face 仍是素髎 `GV25_M NME 9.65%` 那支**已知紅**。
- ✅ `python tests/browser_ui.py`：**344 項全過、零 pageerror**。
  截圖：`比賽專區\介面討論\急症口徑同步_20260920\`、`手臉病症合併_20260920\`。
- ✅ 截圖自己開來看過：16 格在手機是雙欄 8 列、「顏面神經麻痺」沒被裁字；桌面版也正常。
- ❌ **批 34／35／36／37 都還沒部署**，線上仍是 09-20 上午那版。

### ⚠️ 兩端不一致（仍未解）

- **網頁圖冊沒有臉部穴道**（`tab-gallery-info.js` 只反查手部 `SYMPTOM_MAP`，圖冊只列 `ACUPOINTS`）
  ⇒ 新增那四項在網頁圖冊**完全看不到**，只有 App 有。
- 版面差更多了：網頁有桌面版型與即時數據面板，App 沒有。**交件文件別再說「三份同一套」。**

---

## 2026-09-20（續）— App 補上臉部併進療程（批 41），網頁**沒有改動**

這一條記在這裡是為了**校正兩端的落差表**，網頁這份一個字都沒動。

使用者今天要求「臉部穴道變成可選、流程和手部一樣」⇒ App 做了批 41，
**推翻 App 端 2026-09-05 的 A 案**（臉部自成一頁、不進療程）。
App 照抄的就是**這份網頁 2026-09-04 起的既有行為**：

| 網頁（早就有） | App（批 41 才補上） |
|---|---|
| `pages/07-face.js` → `renderFaceList()`：臉部可勾選，存 `state.selectedFace` | `RecommendScreen` 的臉部格子，存 `vm.selectedFace` |
| `pages/03-acu-detail.js` → `buildTreatmentList()`：`[...hands, ...faces]`，**臉部排最後** | `SessionViewModel.buildTreatmentList()`，同一個道理（兩個模型兩頁相機） |
| 認穴頁同一組 DOM 填臉部內容（`renderFaceAcuDetail`） | `AcuDetailScreen` 加臉部分支 |

⇒ **選穴與療程流程兩端從此一致。** 之前寫「臉部在 App 是另一條線」的文件與簡報要改口。

### ⚠️ 兩端不一致（更新版）

- ~~選穴頁臉部能不能勾~~ —— **已一致**（App 批 41）。
- ~~網頁圖冊沒有臉部穴道~~ —— **已一致**（網頁批 42，見下一節）。
- **臉部教學影片兩端都沒有**：網頁 `js/acu-video.js` 的 `ACU_VIDEO` 只有合谷，
  App `AcuVideo.FACE_RAW_NAMES` 是空的。兩邊都是「框架接好了、片沒拍」。
- 版面差更多了：網頁有桌面版型與即時數據面板，App 沒有。**交件文件別再說「三份同一套」。**

---

## 2026-09-20（續 2）— 圖冊納入臉部 23 穴（批 42，**手機版**）

使用者：「**手機版本的網頁先同步，然後電腦版本和平板的我再調整**」。
⇒ 這批只保證**手機（390／320）看起來對**；桌面與平板留給使用者自己調版。

補的是 09-20 上午還列在「兩端不一致」的第一條：網頁圖冊只有手部 26 穴，臉部 23 穴看不到。

| 檔案 | 動了什麼 |
|---|---|
| `js/face-data.js` | 新增 `faceColor(code)` ＋ `FACE_GROUP_HUE`。**與 App 的 `FaceData.hue()` 同一組數字**（眼 262／眉 292／鼻 16／口 336／側臉 92）|
| `js/regions.js` | 新增 `itemColor(id)` —— 手部走 `acuColor()`、臉部走 `faceColor()` |
| `pages/tab-gallery.js` | 改成兩組（手部 26 ＋ 臉部 23），分組標題橫跨整列；抽出 `renderCell()` 兩組共用 |
| `pages/tab-gallery-info.js` | `showAcuInfo()` 收臉部代號；新增 `renderFaceAcuInfo()`；`practiceThisAcu()` 改吃 `itemImplemented()` |
| `js/i18n.js` | `gallery-group-hand` / `gallery-group-face` / `gallery-face-nominion`（中英）|
| `tests/e2e.js` | 圖冊那批斷言全部改寫（26 → 49），新增臉部介紹頁一整段 |
| `tests/browser_ui.py` | **圖冊與臉部介紹頁現在會截圖** —— 只有 overflow 檢查的話，看不出版面好不好看 |

### 幾個刻意的決定

- **臉部沒有小人，也不借手部的。** 那組 SVG 只畫了手部 26 穴；借來用會出現「兩格長一樣」，
  讓人以為是同一穴。改成**代號膠囊**（`ST1`、`EX-HN7`），跟定位頁、介紹頁看到的是同一個識別字。
  格子下方另有一行小字說明「臉部穴道還沒有小人」。
- 膠囊而不是正圓：`EX-HN7` 七個字在正圓裡會換行、溢出 —— 320px 小螢幕尤其明顯。
- 未解鎖的臉部格**不能淡到讀不出代號**（`opacity: .5` 而不是 `.35`）：手部靠小人剪影就認得出，
  臉部沒有小人，代號是唯一線索。
- 臉部介紹頁跟手部**同一頁、同一組 DOM**（跟認穴頁 `renderFaceAcuDetail()` 一樣的做法），
  不另開一頁 —— 兩頁會各自長歪。

### 驗證

- ✅ `node tests/run.js`：**e2e 444 → 459**，其餘全綠。face 仍是素髎 `GV25_M NME 9.65%` 那支**已知紅**。
- ✅ `python tests/browser_ui.py`：**372 項全過**（344 → 372，新增圖冊與臉部介紹頁兩頁 × 七尺寸）、零 pageerror。
- ✅ 截圖自己開來看過：`比賽專區\介面討論\網頁圖冊納入臉部_20260920\`
  （phone / small / large-text / desktop 的 `-gallery` 與 `-gallery-face`）。
- ❌ **沒部署**，線上仍是 09-20 上午那版。

### ⚠️ 留給使用者調的（桌面／平板）

- 桌面 1440 寬下格子被拉得很大，**臉部的代號膠囊在大格子裡顯得很小、很空**。
- 桌面截圖裡有幾格手部小人是空的（四縫、少商、商陽…）—— 那是 full_page 截圖當下 `loading="lazy"`
  還沒載完，**不是缺圖**；手機版截圖同樣那幾格都有小人。

---

## 2026-09-20（續 3）— 開場動畫（批 44，網頁）

使用者丟了一張圖：「**把這個當作進入 app 后的開場動畫**」。
照 08-18 的流程（新功能先網頁 → 使用者說可以 → 才進 App），這批**只做網頁**。

### 素材

| | |
|---|---|
| 原檔 | `比賽專區\素材\界面設計\Codex 图像 2026年9月20日 20_37_53.gif`（468×832、27 幀、約 3.2 秒、**5.27 MB**）|
| 網站用 | `assets/splash/opening.webp`（animated WebP、**816 KB**）＋ `opening-still.webp`（最後一幀，37 KB）|

⭐ **轉成 animated WebP，小了 85%。** GIF 對這種有漸層天空的插畫效率極差；
5 MB 的「開場」等於開場自己變成等待，線上版（GitHub Pages）尤其明顯。
轉檔參數：`quality=80, method=6`，尺寸與逐幀 duration 都照原檔。**原始 GIF 留在素材資料夾，不進網站。**

### 行為

| | |
|---|---|
| 何時播 | 每次載入頁面，`boot()` 的最後一行（頁面已經組好，開場只是蓋在上面 → 收掉的瞬間首頁是完整的，不會閃白）|
| 多久 | 動畫本身 3.17 秒，收場淡出 0.42 秒 |
| 跳過 | **整片都能點**。不畫「跳過」按鈕 —— 整片比一顆小按鈕好按，也省掉中英文案與擺位 |
| 減少動態 | `prefers-reduced-motion: reduce` 時**整層根本不建**（不是建了再隱藏）|
| 載不出來 | `onerror` 退最後一幀的靜態圖；靜態圖也失敗就**直接收掉** |
| 保險絲 | `SPLASH_MAX_MS = 5000`，不管圖載好沒都強制收 |

🔴 **唯一的硬規則：開場動畫永遠不可以擋住人進 App。** 上面最後三列都是為了這一條。

⚠️ 不用 `<video>`：自動播要 muted+playsinline，iOS 低電量模式仍可能不給播；
`<img>` 沒有這些限制，3 秒的短片用影片編碼也省不了多少。

### 驗證

- ✅ `node tests/run.js`：**e2e 459 → 467**（新增 8 項：建得出來、用的是 WebP 不是 5MB 的 GIF、
  點一下淡出、淡出後真的移除、重複呼叫安全、兩段 `onerror` 退路）。
- ✅ `python tests/browser_ui.py`：**381 項全過**（372 → 381）、零 pageerror。
  ⚠️ 既有那七個尺寸的 context **全部帶 `reduced_motion='reduce'`**，所以開場在那裡根本不會出現 ——
  也就是說那 372 項**一項都沒碰到開場**。新加的檢查另開 context 專門跑它
  （`phone` 真的播、`desktop` 真的播、`phone-reduced` 確認不播）。
- ✅ 截圖自己開來看過：`比賽專區\介面討論\開場動畫_20260920\phone-splash.png`、`desktop-splash.png`。
  桌面是中間直式 ＋ 兩側天空藍底（底色 `#7ec8f0` 跟素材天空幾乎一致，銜接不突兀）。
- ❌ **沒部署**。
- ✅ App 同日晚間已跟上（App 批 45，見下一節）。

### App 那一半（同日晚間已做 —— App 批 45）

用戶看過網頁這版後說「**app 也加入看看效果**」⇒ 08-18 流程第 ③ 步。
細節見 `比賽專區\AcuNavi\README.md`，這裡只記兩端的差別：

| | 網頁 | App |
|---|---|---|
| 播放 | `<img>` 直接吃 animated WebP | API 28+ `ImageDecoder` → `AnimatedImageDrawable`；**24～27 退靜態圖** |
| 減少動態 | `prefers-reduced-motion` | `Settings.Global.ANIMATOR_DURATION_SCALE == 0` |
| 不重播 | 沒處理（重新整理就重播） | `rememberSaveable` —— **轉螢幕、切語言不重播** |

⭐ 三個時間常數（`SPLASH_MS` / `SPLASH_FADE_MS` / `SPLASH_MAX_MS`）**兩端必須一致**，
App 那邊有一條測試**直接讀這份 `js/splash.js`** 比對 —— 改素材只改一邊會直接紅燈。

---

## 2026-09-20（續 4）— 首頁同步草地背景＋玻璃症狀格（批 49）

使用者：「**手機版本網頁做同步**」⇒ 把 App 批 46–48 的首頁外觀搬過來。

| 檔案 | 動了什麼 |
|---|---|
| `assets/home-bg.webp` | **新素材**，73 KB，**與 App 的 `res/drawable-nodpi/home_bg.webp` 是同一份** |
| `css/shell.css` | `.device::before` 鋪整個殼、`opacity .55`；深色壓到 `.3`；`.device > *` 提到 `z-index: 1` |
| `css/responsive.css` | 症狀鈕改玻璃（`.44` / `.66`、白邊 `.8`、**拿掉陰影**）；拿掉 `.handmark`；**拿掉 `.home-top` 的白底**；深色改半透明黑 |
| `pages/01-home.js` | 移除 `<img class="handmark">` |
| `js/nav.js` | `showPage()` 把目前頁標到 `document.body.dataset.page` |

⭐ **三個數字與 App 同一組**：背景 `.55`、未選中 `.44`、選中 `.66`。改一邊就要改另一邊。

🚨 **為什麼畫在 `.device::before` 而不是 `#page-home`**：頁面只佔 `main` 那一塊，
上面 header、下面分頁列、左右內距 —— 畫在頁面裡四周仍是一圈白邊（App 端批 46 就是這樣）。

🚨 **截圖才抓到的**：`#page-home .home-top` 有 `background: var(--surface)`。
第一版 **381 項檢查全過**，但截圖上**只有邊緣看得到草地、中間一整塊還是白的**。
那層白原本也是用來擋手掌底圖的。**又一次證明「檢查全綠不等於版面對了」。**

🚨 **深色模式對比不足（兩端都中）**：半透明**白**玻璃配深色主題的淺色字＝淺字配淺底。
→ 深色改成半透明黑（`.34` / `.5`）、邊框 `rgba(255,255,255,.3)`。App 同一天用
`isSystemInDarkTheme()` 修了同一個問題。

---

## 2026-09-21 — iPad 直式直接套手機版型（批 50）

使用者：「對就是 ipad 直接套用手機的那個模板，**橫屏我考慮下**」⇒ **只動直式**。

⭐ **判準改用 `orientation` 而不是寬度**：iPad 直式是 820×1180，寬度上跟小筆電分不開。
→ 兩組寬版型（`760px` 首頁兩欄、`1024px` 桌面左側導覽欄）全部加
`and (orientation: landscape)`，直式自動落回手機那一套。

再補一條「手機模板置中放大」：

```css
@media (orientation: portrait) and (min-width: 600px) {
  body .device { max-width: 560px; border-inline: 1px solid var(--line-soft); }
}
```

⚠️ 不限寬的話 `body .device` 是 1120px，單欄內容被拉成一條很寬的帶子，**比兩欄還難看**。

| 尺寸 | 之前 | 現在 |
|---|---|---|
| iPad 直式 820×1180 | 首頁兩欄（右欄 aside）| **手機版型，560px 置中** |
| iPad 橫式 1180×820 | 桌面版（左側導覽欄）| **不變**（使用者考慮中）|
| 桌面 1440×900 | 桌面版 | 不變 |

⚠️ **副作用**：桌面把瀏覽器視窗拉成細長，現在也會套手機版型。
這是 orientation 判準的必然結果 —— 判斷是合理的，但它是副作用不是需求。

### 這兩批的驗證

- ✅ `node tests/run.js`：**e2e 467 項**全過（兩批都是純 CSS，沒新增）；face 仍是素髎已知紅。
- ✅ `python tests/browser_ui.py`：**381 項全過**、零 pageerror。
- ✅ 截圖自己開來看過：`比賽專區\介面討論\首頁草地背景_20260920\`（phone／tablet／**phone-dark**）、
  `比賽專區\介面討論\iPad直式套手機版_20260921\`（`tablet-home` 已是手機版、`desktop-home` 未受影響）。
- ❌ **沒部署**；**沒有真 iPad 實測**（Playwright 的 820×1180 只是視窗大小，不是真裝置）。

---

## 2026-09-21（續）— 對齊 App 四項、藍帶收高、症狀六列加高（批 51–53）

用戶在手機開線上版比對 App，連提三輪。**全部只動網頁，App 一個字沒改。**

### ⓪ 先發現的其實是「線上版落後兩天」

線上（GitHub Pages）停在 `8034a09`＝09-16～09-19 那批。**09-20～09-21 的六批全沒上線**：
開場動畫（批 44）、草地背景＋玻璃格（批 49）、iPad 直式（批 50）、急症口徑、手臉病症合併、桌面版型重設計。
⇒ 用戶看到的「網頁跟 App 不一樣」有一半是這個造成的。**先原樣部署（`7bbae10`）再動手。**

🔴 **教訓：改完要記得跑 `比賽專區/發布/部署.ps1`。** 本機測試全綠不代表用戶看得到。

### ① 🚨 底部分頁列跑到畫面外 —— 是回歸，不是沒做（批 51）

`css/shell.css` 的 `.device > * { position: relative; z-index: 1 }`（09-20 加草地背景時寫的）
與上面 `.tabbar { position: sticky }` **特異性同為 (0,1,0)**，寫在後面就贏 ⇒ sticky 被蓋掉。
實測 phone 390×844 上分頁列 `top=844`（正好在視窗外），要往下捲 90px 才看得到。

→ 改成 `.device > *:not(.tabbar)`。tabbar 自己有 `z-index: 20`，且 sticky 本來就建立堆疊脈絡，
不需要靠那條站到草地背景之上。桌面版（≥1024px）另有明確的 `position: static`，不受影響。

🔴 **這是本月第二次栽在 CSS 特異性，兩次都沒有任何錯誤訊息**（上次是 09-19 的 `:is()`
取括號內最強項）。兩次的症狀都是「我明明寫了卻沒生效」，而且——
**574 項單元測試全綠、381 項多端檢查也全綠，沒有一項抓得到。**
`tests/browser_ui.py` 只驗橫向捲軸與文字裁切，**不驗元素在不在視窗裡**。

### ② 設定目錄同步 App（批 51）

見上面〈設定：一個功能一頁〉那張目錄圖。新檔 `pages/settings-display.js`（字體大小）。
倍率 **0.85 / 1.0 / 1.15 與 App 的 `FontScale`（`data/Progress.kt`）是同一組數字**，
做法是改 `<html>` 的 `font-size`，全站 rem 跟著縮放 ⇒ **只有字在變**，px 寫死的邊框間距不動。
⚠️ 所以 css 裡的字級要用 rem，寫 px 的字不會跟著這個設定跑。

### ③ 圖冊 4 欄 → 3 欄（批 51）

App 是 `GridCells.Fixed(3)`。欄少一欄＝每格寬約多三成，穴名 9.5 → 11.5px、臉部代號 8.5 → 10.5px。
⚠️ **那段註解不要用反引號** —— 它在 template literal 裡面，寫了 `GalleryScreen.kt` 會把字串切斷
（當場踩到，`Unexpected identifier 'GalleryScreen'`）。

### ④ 首頁藍帶收高 24px（批 52）

用戶：「主訴那裏，那一大塊高度太高了，主訴和選擇你的症狀中間不要有空格」。

量出來的：`.home-heading` 那一行 **54px 高，但 h2 只有 30px** —— 多出來的 24px 全是右邊
那排小人的站位撐的（`.home-cast` 固定 54px ＋ `align-items: end` ⇒ 標題被壓到行底）。
**一個症狀都沒選時那 24px 完全是空的。**

→ `.home-cast` 改 `height: 0; overflow: visible;`：行高由 h2 決定，小人仍靠 `align-items: end`
對齊標題底邊、往上長溢出到行外。藍帶 132.8 → **108.8px**，省下的高度回到症狀區。

⚠️ 小人往上長 48px，與「主訴」垂直上重疊 12px（desktop 4.5px）但**水平分離**（小人在右、主訴在左），
截圖確認沒有疊字。這個餘裕跟 `.eyebrow` 的 `margin-bottom: 6px` 與 h2 的 `line-height` 綁著。

### ⑤ 症狀格改六列加高、分頁列再拉低（批 53）

用戶：「下面首頁那些選擇欄位幫我拉低一點點，然後病症選擇的格子拉高些，
如果有必要甚至可以變成病症先顯示六行」。

| | 09-21 早上 | 批 51（七列） | 批 53（現在） |
|---|---|---|---|
| 可見列數 | 8 列全擠 | 7 | **6** |
| 列高 phone | 48（不捲，整頁變長） | 42 | **55px** |
| 列高 tablet | — | 93 | **114px** |
| 分頁列離底邊 | 12px | 12px | **6px** |

⭐ **改成六列剛好跟 App 的 `BASELINE_ROWS`（本來就是 6）對上了** —— 批 51 那版七列反而比 App 多一行。

🔴 **保底列高 42px → 48px 的來歷**：做七列時 48px 是不可能的 ——
iPhone 390×844 的症狀區只有 343px，`(343 − 6×8)/7 = 42.1px`；保底寫 48px 的話七列共 384px > 343px，
**第七列被擠出捲動區、畫面上只剩六列**（第一版就是這樣，靠 probe 量 `visibleRows` 才抓到）。
改六列之後高度夠了（約 55px/列），保底才回得到 48px 這個觸控目標下限。

⚠️ 分頁列的 `margin-bottom` 與 `bottom` **必須同一個值**：前者管文件流底部留白，後者管 sticky
貼在視窗底多高。只改一個的話，捲到底時膠囊會自己跳一下。
`env(safe-area-inset-bottom)` 那層留著（iPhone home indicator 會壓到膠囊）。

### 驗證

- `node tests/run.js` **574 項全過**。e2e 改了 4 條寫死舊設定目錄的斷言、新增 7 條字級斷言。
  素髎 `GV25_M NME 9.65%` 是**既有失敗**（臉部公式凍結中），與這三批無關。
- `python tests/browser_ui.py` **381 項全過 0 FAIL**，三輪各自歸檔：
  `比賽專區/介面討論/對齊App設定與圖冊_20260921/`、`主訴藍帶收高_20260921/`、`症狀六列格子加高_20260921/`。
- 🔴 **另外寫了一次性 probe** 量「症狀格可見列數」「分頁列的螢幕座標」「hero band 各盒子的 top/bottom」——
  **這三件事上面兩套都驗不到**。①那個回歸、42px 保底擠掉第七列、藍帶那 24px，
  全是這支 probe 抓出來的。下次動版面高度，照樣得自己量。
- **實機沒驗**（用戶自己在手機上看）。

### 還沒處理

1. **桌面 1440×900 上症狀格只有 6 列、下方一大片草地空白** —— 兩欄版型高度不夠，這三輪講的都是手機。
2. **六列之後沒有「還有更多」的視覺提示**（16 項＝8 列，第 7、8 列看不到）。App 也沒有，所以保持一致。
3. **App「顯示」頁的骨架開關網頁沒有**（網頁不畫 21 點）。

## 批 54（2026-09-21）｜平板直式留白過多

`css/responsive.css` 的 `@media (orientation: portrait) and (min-width: 600px)`：
`body .device` max-width **560px → 900px**，移除 `border-inline`。

批 50 寫 560px 的理由是「不限寬會被拉成一條很寬的帶子」，但那件事
`.device main > .page > .stack { max-width: 760px }` 本來就擋掉了 ——
560 只是讓 820×1180 的平板兩側各空 130px 灰底。放寬後藍帶／草地背景／分頁列滿寬，段落照舊 760px。

驗證：`node tests/run.js` 574 項（素髎 GV25_M 為既有失敗）、`tests/browser_ui.py` 381 項全過 0 FAIL，
截圖 `比賽專區/介面討論/平板留白_20260921/`。實機沒驗。

⚠️ 只修橫向。垂直留白（選穴頁下半約 400px、按摩頁計時卡只佔左半）待決定。

## 批 56（2026-09-21）｜平板藍帶下半段比 header 窄

`css/responsive.css` 的 `@media (orientation: portrait) and (min-width: 600px)` 加兩條：

```css
#page-home > .stack { max-width: none; }
#page-home .hero-band { margin-inline: -18px; padding-inline: 18px; }
```

改前：tablet 帶 809 / header 820、iPad Pro 帶 821 / header 900 —— 畫面上是「深藍分兩塊、下面那塊較窄」。
兩因疊加：①`.hero-band` 出血寫死 `clamp(16px,3vw,32px)`，跟批 50 改成 18px 的內距對不上；
②`#page-home > .stack` 的 760px 上限。**只改一條都還是會差。**
改後兩個尺寸都精準齊平，手機 390 不受影響。

⚠️ `.hero-band` 的出血數字與 `.device main` 的內距是**綁死的**（原註解已寫，批 50 仍漏跟）。
之後任何斷點動 main 內距，都要回來看這條。

驗證：`node tests/run.js` 574 項（素髎既有失敗）、`tests/browser_ui.py` 381 項全過 0 FAIL，
截圖 `比賽專區/介面討論/平板藍帶對齊_20260921/`。實機沒驗。

## 2026-09-22｜手機圖冊外觀對齊 App

- 三欄卡片改成 App 的直向節奏：56px 小人、穴名、解鎖狀態。
- 手部、臉部與前臂統一使用 App 的手掌小人；臉部不再顯示代號膠囊。
- 加入前臂 15 穴展示組，總數 49 → 64；前臂仍標示尚未開放定位。
- e2e 全過；七種尺寸 browser UI 全過。截圖在 `比賽專區/介面討論/網頁圖冊對齊App_20260922/`。
- 既有素髎 GV25_M NME 9.65% 測試仍失敗，與本次圖冊改版無關。

## 2026-09-22｜手機網頁功能續同步 App

- 首頁症狀 16 → 20，補入 App 的前臂四項症狀。
- 前臂 15 穴共用 App 展示資料與參考圖；圖冊介紹頁可閱讀定位、用途、按法與警語，但練習按鈕維持停用。
- 選穴頁的手肘分頁改為可開啟的唯讀清單；前臂專屬症狀會自動落在手肘分頁，不會把尚未定位的穴道排進療程。
- 手機的選穴詳情與圖冊介紹改為底部面板；桌面與平板原版型不變。
- 手機手部定位顯示加入與 App 相同參數的 One Euro 平滑，以及既有閘門擋下時的水平回正箭頭。平滑只影響繪圖，定位與按摩判定仍使用原始座標。
- e2e 全過；七種尺寸 browser UI 全過、無 page error。`tests/run.js` 聚合器在目前 D 槽中文路徑環境無法啟動子程序，逐支執行皆可；臉部公式仍只有既有的素髎 `GV25_M NME 9.65%` 超過 8%。

## 2026-09-22｜讀數條改疊在取景框內（批 59 網頁側）

用戶：「那些提示在鏡頭畫面下方，他會壓縮到我的鏡頭畫面，先去確認然修改」。
App 側的對應改動見 `比賽專區/AcuNavi/README.md` 的〈批 59〉。

⭐ **網頁的機制跟 App 不一樣，別誤判**：
- 手機／直式：canvas 吃 `aspect-ratio: 4/3`，讀數條其實**沒有**壓縮畫面，只是讓整頁多佔 45px 高。
- 桌面橫式：`responsive.css` 用 `max-width: calc((100svh − N) * 4/3)` 限寬，
  而那個 N **明確含「下方狀態條」的概估** ⇒ 讀數條在這裡是**間接**壓縮畫面的。
  故 N 由 **300px → 255px**，畫面跟著變大。**這兩處是綁在一起的，改一邊要改另一邊。**
- 按摩頁全螢幕（`body.massage-fs`）：2026-08-13 起讀數條本來就搬進畫面下緣疊加，
  這次等於讓**非全螢幕**也對齊全螢幕那套做法。

改動：
- `pages/04-camera.js`、`pages/05-massage.js`：讀數條 DOM **搬進 `.viewport` 裡面**（原為兄弟節點）
- `css/shell.css`：新增 `.viewport > .readout` 疊加樣式，深底 `rgba(11,18,16,.8)`，
  語氣色只換文字與圓點（黑底上淺色語氣底會糊掉），與 App 的 `OverlayNotice` 同色值
- `css/responsive.css`：桌面橫式限寬常數 300px → 255px

### 🚨 這批踩到的兩個坑

**① 定位基準選錯，409 項檢查全過卻是壞的**
第一版把基準放在外層 `.camera-preview` / `.massage-preview` ⇒ 桌面橫式時
**讀數條超出取景框右緣約 30px**（`.viewport` 有 `max-width` 限寬、外層沒有）。
⚠️ `browser_ui.py` **409 項全過、一項都沒抓到** —— 它只驗橫向捲軸與文字裁切，
**「元素跑出容器」驗不到**。是打開截圖才看見的。

**② JS 模板字串的 HTML 註解裡不能寫反引號**
這兩頁整段是模板字串，註解裡用反引號包程式碼名稱 ⇒ **提前關掉模板字串，整頁靜默不生成**
（`#massage-gate` 根本不存在，Playwright 等 30 秒逾時）。
`node --check pages/*.js` 一秒抓到，但 **`node tests/run.js` 沒有這道檢查**。

驗證：`tests/browser_ui.py` **409 項全過 0 FAIL**（七尺寸 × 中英 × 深色），**截圖逐張看過**，
歸檔 `比賽專區/介面討論/提示疊加取景框_20260922/`。
`node tests/run.js` 只有素髎 `GV25_M NME 9.65%` 一支紅 —— **把本次 CSS stash 掉重跑照樣紅**，
確認是既有失敗，非本次引入。

⚠️ 臉部頁 `#face-gate` **刻意沒動**，所以定位頁／按摩頁／臉部頁目前不一致。

---

## 2026-09-23 — 對位判定補遲滯（bug 修正，對齊 App）

以前網頁只有 `onTarget = minD <= tol`，手指停在邊界時手一抖，計時就 on/off 反覆跳；App 早就有遲滯。
（分岔是 `記錄控制/系統實際能力盤點_20260921.md` 第一節盤點出來的。）

- `js/vision.js`：新增 `pressOnTarget(minD, tolIn, wasOn)` —— 已對準時要超出 **1.6 × tolIn** 才判離開，同 App `AcuMath.press`。
- 記憶 `pressWasOn` **每幀先歸零**，只有走到按摩對位判定才寫入 ⇒ 手離開畫面、閘門擋下、換頁都不會帶舊狀態。
- `tests/e2e.js` +5 項（481 → 486）。`node tests/run.js` 只剩既有的素髎 `GV25_M` 一支紅。

⚠️ **沒用真相機試過；尚未部署到 bisai。**
⚠️ 臉部按摩的「貼到臉」判定 App 與網頁**都沒有遲滯**（兩邊一致，不算分岔），沒動。
