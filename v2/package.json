{
  "name": "acunavi-demo",
  "version": "1.0.0",
  "private": true,
  "description": "AcuNavi 資服競賽 demo 網站。網站本身不需要建置，package.json 只為了測試用的 jsdom。",
  "scripts": {
    "test": "node tests/run.js",
    "test:verbose": "node tests/run.js --verbose",
    "serve": "python -m http.server 8000"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
